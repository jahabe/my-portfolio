import React from "react";

const AiClassifier = () => {
  return (
    <div style={{ padding: "80px 40px", color: "#333" }}>
      <h1>AI-Powered Image Classifier with PyTorch</h1>
      <p>
        This project is part of Udacity's AI Programming with Python Nanodegree.
        It uses PyTorch and VGG16 for classifying flower images, featuring command-line interface,
        GPU acceleration, and flexible dataset integration.
      </p>
    </div>
  );
};

export default AiClassifier;
